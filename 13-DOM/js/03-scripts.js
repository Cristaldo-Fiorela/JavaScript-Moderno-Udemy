// Tomar un elemento por ID
const formulario = document.getElementById('formulario');
console.log(formulario);

// Si no existe
const noExiste = document.getElementById('no-existe');
console.log(noExiste) // <== null
